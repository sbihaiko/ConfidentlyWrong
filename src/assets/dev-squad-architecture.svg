<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1520 1060" width="1520" height="1060" font-family="-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif" role="img" aria-label="dev-squad architecture: one command runs a self-reviewing, test-gated pipeline with an adversarial actor-critic loop that learns your project over time">
  <defs>
    <!-- blue pipeline arrow head -->
    <marker id="aBlue" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
      <path d="M0,0 L10,5 L0,10 z" fill="#0046F5"/>
    </marker>
    <!-- green memory arrow head -->
    <marker id="aGreen" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7.5" markerHeight="7.5" orient="auto-start-reverse">
      <path d="M0,0 L10,5 L0,10 z" fill="#0F9D58"/>
    </marker>
    <!-- navy loop arrow head -->
    <marker id="aNavy" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6.5" markerHeight="6.5" orient="auto-start-reverse">
      <path d="M0,0 L10,5 L0,10 z" fill="#00113A"/>
    </marker>
    <!-- red reject arrow head -->
    <marker id="aRed" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6.5" markerHeight="6.5" orient="auto-start-reverse">
      <path d="M0,0 L10,5 L0,10 z" fill="#C0392B"/>
    </marker>
    <!-- grey optional arrow head -->
    <marker id="aGrey" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6.5" markerHeight="6.5" orient="auto-start-reverse">
      <path d="M0,0 L10,5 L0,10 z" fill="#94A3B8"/>
    </marker>
    <filter id="cardShadow" x="-20%" y="-20%" width="140%" height="140%">
      <feDropShadow dx="0" dy="2" stdDeviation="6" flood-color="#00113A" flood-opacity="0.10"/>
    </filter>
    <filter id="softShadow" x="-20%" y="-20%" width="140%" height="140%">
      <feDropShadow dx="0" dy="1" stdDeviation="3" flood-color="#00113A" flood-opacity="0.08"/>
    </filter>
  </defs>

  <!-- ============ THEME-SAFE BACKGROUND ============ -->
  <rect x="0" y="0" width="1520" height="1060" fill="#F8F9FA"/>
  <rect x="0.5" y="0.5" width="1519" height="1059" fill="none" stroke="#E1E5EE" stroke-width="2"/>

  <!-- ============ HEADER ============ -->
  <text x="64" y="58" font-size="13" font-weight="700" letter-spacing="3" fill="#0046F5">DEV-SQUAD · CLAUDE CODE PLUGIN</text>
  <text x="64" y="96" font-size="30" font-weight="700" letter-spacing="-0.5" fill="#00113A">One command runs a self-reviewing, test-gated dev pipeline</text>
  <text x="64" y="124" font-size="15" font-weight="400" fill="#64748B">An adversarial actor⇄critic loop builds it, exit-code tests gate the commit, and every run teaches your project memory.</text>

  <!-- ===================================================================== -->
  <!-- ============ L2 ENGINE BAND BACKGROUND ============ -->
  <!-- band x=316 .. 1456 (right margin), even 30px pad around its pipeline cards -->
  <!-- ===================================================================== -->
  <rect x="316" y="176" width="1140" height="140" rx="18" fill="#0046F5" fill-opacity="0.04" stroke="#0046F5" stroke-opacity="0.30" stroke-width="1.5" stroke-dasharray="2 5"/>
  <text x="346" y="200" font-size="11" font-weight="700" letter-spacing="1.5" fill="#0046F5">L2 · WORKFLOW ENGINE — background, journaled, resumable</text>

  <!-- ===================================================================== -->
  <!-- ============ COMMAND ENTRY (L1) ============ -->
  <!-- widened to 224 so the mono command has >=12px clearance both sides ============ -->
  <!-- ===================================================================== -->
  <g filter="url(#cardShadow)">
    <rect x="64" y="224" width="224" height="62" rx="12" fill="#00113A"/>
  </g>
  <text x="80" y="249" font-size="10.5" font-weight="700" letter-spacing="1.3" fill="#7FA6E6">ENTRY · L1 PLUGIN SHELL</text>
  <text x="80" y="272" font-size="13" font-weight="700" fill="#FFFFFF" font-family="'SF Mono','Roboto Mono',Menlo,Consolas,monospace">/dev-squad:squad &lt;task&gt;</text>

  <!-- entry -> Scout connector (orthogonal, straight H) -->
  <path d="M288,255 H 344" stroke="#0046F5" stroke-width="2.5" fill="none" marker-end="url(#aBlue)"/>

  <!-- ===================================================================== -->
  <!-- ============ PIPELINE STAGES (L2) — EVENLY DISTRIBUTED ============ -->
  <!-- mid y=255, cards y=224 h=62. Even 40px gaps across the full band width. -->
  <!-- Scout 346 | Spec 514 | Decompose 682 | Execute 850(w168) | Verify 1058 | gate 1246 | Commit 1306 -->
  <!-- ===================================================================== -->

  <!-- SCOUT -->
  <g filter="url(#cardShadow)"><rect x="346" y="224" width="128" height="62" rx="12" fill="#FFFFFF" stroke="#E1E5EE" stroke-width="1.5"/></g>
  <text x="410" y="249" font-size="12" font-weight="700" fill="#00113A" text-anchor="middle">Scout</text>
  <text x="410" y="266" font-size="9.5" fill="#64748B" text-anchor="middle">detect stack,</text>
  <text x="410" y="278" font-size="9.5" fill="#64748B" text-anchor="middle">classify, load context</text>

  <!-- SPEC -->
  <g filter="url(#cardShadow)"><rect x="514" y="224" width="128" height="62" rx="12" fill="#FFFFFF" stroke="#E1E5EE" stroke-width="1.5"/></g>
  <text x="578" y="249" font-size="12" font-weight="700" fill="#00113A" text-anchor="middle">Spec</text>
  <text x="578" y="266" font-size="9.5" fill="#64748B" text-anchor="middle">acceptance</text>
  <text x="578" y="278" font-size="9.5" fill="#64748B" text-anchor="middle">criteria</text>

  <!-- DECOMPOSE -->
  <g filter="url(#cardShadow)"><rect x="682" y="224" width="128" height="62" rx="12" fill="#FFFFFF" stroke="#E1E5EE" stroke-width="1.5"/></g>
  <text x="746" y="249" font-size="12" font-weight="700" fill="#00113A" text-anchor="middle">Decompose</text>
  <text x="746" y="266" font-size="9.5" fill="#64748B" text-anchor="middle">1–5 ordered</text>
  <text x="746" y="278" font-size="9.5" fill="#64748B" text-anchor="middle">tasks</text>

  <!-- EXECUTE (hero of pipeline, blue border) -->
  <g filter="url(#cardShadow)"><rect x="850" y="224" width="168" height="62" rx="12" fill="#FFFFFF" stroke="#0046F5" stroke-width="2.5"/></g>
  <text x="934" y="249" font-size="12" font-weight="700" fill="#0046F5" text-anchor="middle">Execute</text>
  <text x="934" y="266" font-size="9.5" fill="#64748B" text-anchor="middle">parallel tasks in</text>
  <text x="934" y="278" font-size="9.5" fill="#64748B" text-anchor="middle">isolated git worktrees</text>

  <!-- VERIFY -->
  <g filter="url(#cardShadow)"><rect x="1058" y="224" width="128" height="62" rx="12" fill="#FFFFFF" stroke="#E1E5EE" stroke-width="1.5"/></g>
  <text x="1122" y="249" font-size="12" font-weight="700" fill="#00113A" text-anchor="middle">Verify</text>
  <text x="1122" y="266" font-size="9.5" fill="#64748B" text-anchor="middle">re-run ACs +</text>
  <text x="1122" y="278" font-size="9.5" fill="#64748B" text-anchor="middle">full test suite</text>

  <!-- COMMIT (gated terminal) -->
  <g filter="url(#cardShadow)"><rect x="1306" y="224" width="120" height="62" rx="12" fill="#00113A"/></g>
  <text x="1366" y="251" font-size="12" font-weight="700" fill="#FFFFFF" text-anchor="middle">Commit</text>
  <text x="1366" y="270" font-size="10" fill="#93C63E" text-anchor="middle">on green</text>

  <!-- pipeline connectors (orthogonal straight H, each at card mid y=255) -->
  <path d="M474,255 H 512" stroke="#0046F5" stroke-width="2.5" fill="none" marker-end="url(#aBlue)"/>
  <path d="M642,255 H 680" stroke="#0046F5" stroke-width="2.5" fill="none" marker-end="url(#aBlue)"/>
  <path d="M810,255 H 848" stroke="#0046F5" stroke-width="2.5" fill="none" marker-end="url(#aBlue)"/>
  <path d="M1018,255 H 1056" stroke="#0046F5" stroke-width="2.5" fill="none" marker-end="url(#aBlue)"/>

  <!-- ===== DETERMINISTIC GATE between Verify and Commit ===== -->
  <!-- Verify right edge (x=1186) -> blue arrow lands ON the diamond's LEFT vertex (x=1226) -->
  <!-- diamond center x=1246, half-width 20 -> left vertex 1226, right vertex 1266 -->
  <path d="M1186,255 H 1224" stroke="#0046F5" stroke-width="2.5" fill="none" marker-end="url(#aBlue)"/>
  <polygon points="1246,239 1266,255 1246,271 1226,255" fill="#FFFFFF" stroke="#0F9D58" stroke-width="2.5"/>
  <text x="1246" y="259" font-size="10" font-weight="700" fill="#0F9D58" text-anchor="middle">✓?</text>
  <!-- gate -> Commit: green pass arrow from diamond right vertex to Commit left edge -->
  <path d="M1266,255 H 1304" stroke="#0F9D58" stroke-width="2.5" fill="none" marker-end="url(#aGreen)"/>
  <text x="1285" y="246" font-size="9" font-weight="700" fill="#0F9D58" text-anchor="middle">pass</text>

  <!-- ===================================================================== -->
  <!-- ============ graphify OPTIONAL INPUT (far-left; feeds Scout) ============ -->
  <!-- placed in the clear left margin below the L1 entry; routes up its own -->
  <!-- left channel into Scout's bottom port — never touches the green memory -->
  <!-- channel (which rises at x=320). -->
  <!-- ===================================================================== -->
  <g filter="url(#softShadow)"><rect x="64" y="346" width="224" height="84" rx="12" fill="#FFFFFF" stroke="#94A3B8" stroke-width="1.5" stroke-dasharray="6 4"/></g>
  <rect x="80" y="358" width="74" height="22" rx="11" fill="#EEF1F6"/>
  <text x="117" y="373" font-size="9" font-weight="700" letter-spacing="0.5" fill="#64748B" text-anchor="middle">OPTIONAL</text>
  <text x="164" y="374" font-size="12" font-weight="700" fill="#00113A">graphify</text>
  <text x="80" y="398" font-size="9.5" fill="#64748B">local knowledge graph · graphify-out/</text>
  <text x="80" y="414" font-size="9.5" fill="#64748B" font-family="'SF Mono','Roboto Mono',Menlo,Consolas,monospace">query · explain · path</text>

  <!-- graphify -> Scout : up the left x=96 channel to y=306, right to x=386, up into -->
  <!-- Scout's bottom (port x=386, distinct from the green memory riser at x=410). -->
  <path d="M96,346 V 306 H 386 V 286" stroke="#94A3B8" stroke-width="2" fill="none" marker-end="url(#aGrey)"/>
  <rect x="156" y="296" width="166" height="20" rx="10" fill="#FFFFFF" stroke="#94A3B8" stroke-width="1.2"/>
  <text x="239" y="310" font-size="9" font-weight="600" fill="#64748B" text-anchor="middle">answers codebase Qs for Scout</text>

  <!-- ===================================================================== -->
  <!-- ============ ADVERSARIAL ACTOR ⇄ CRITIC LOOP (the differentiator) ============ -->
  <!-- centered under Execute (mid x=934). widened container; header split to two -->
  <!-- lines so ALL text sits inside with generous margin and never touches the corner. -->
  <!-- ===================================================================== -->
  <!-- connector: Execute bottom-center down to loop box top (orthogonal vertical) -->
  <path d="M934,286 V 453" stroke="#0046F5" stroke-width="2.5" fill="none" marker-end="url(#aBlue)"/>
  <rect x="904" y="424" width="60" height="20" rx="10" fill="#FFFFFF" stroke="#0046F5" stroke-width="1.2"/>
  <text x="934" y="438" font-size="9" font-weight="700" fill="#0046F5" text-anchor="middle">per task</text>

  <!-- loop container: x=674 w=520 (centered on 934), y=455 h=212 -->
  <g filter="url(#cardShadow)"><rect x="674" y="455" width="520" height="212" rx="16" fill="#FFFFFF" stroke="#0046F5" stroke-width="2"/></g>
  <!-- header on TWO lines: bold title + a small sub-label, both fully inside -->
  <text x="700" y="484" font-size="12.5" font-weight="700" letter-spacing="0.8" fill="#0046F5">ADVERSARIAL ACTOR ⇄ CRITIC LOOP</text>
  <text x="700" y="501" font-size="10" font-weight="600" letter-spacing="0.3" fill="#64748B">runs in each worktree, in parallel</text>

  <!-- ACTOR card -->
  <g filter="url(#softShadow)"><rect x="700" y="514" width="178" height="92" rx="12" fill="#F8F9FA" stroke="#CBD5E1" stroke-width="1.5"/></g>
  <text x="789" y="540" font-size="14" font-weight="700" fill="#00113A" text-anchor="middle">Actor</text>
  <text x="789" y="560" font-size="10" fill="#64748B" text-anchor="middle">reads AGENTS.md chain,</text>
  <text x="789" y="574" font-size="10" fill="#64748B" text-anchor="middle">implements the change,</text>
  <text x="789" y="588" font-size="10" fill="#64748B" text-anchor="middle">then runs a DOX pass</text>

  <!-- CRITIC card -->
  <g filter="url(#softShadow)"><rect x="990" y="514" width="178" height="92" rx="12" fill="#F8F9FA" stroke="#CBD5E1" stroke-width="1.5"/></g>
  <text x="1079" y="540" font-size="14" font-weight="700" fill="#00113A" text-anchor="middle">Critic</text>
  <text x="1079" y="560" font-size="10" fill="#64748B" text-anchor="middle">reviews against a</text>
  <text x="1079" y="574" font-size="10" fill="#64748B" text-anchor="middle">per-dimension rubric</text>
  <text x="1079" y="588" font-size="10" fill="#64748B" text-anchor="middle">until convergence</text>

  <!-- Actor -> Critic : top channel, distinct upper port -->
  <path d="M878,536 H 988" stroke="#00113A" stroke-width="2.2" fill="none" marker-end="url(#aNavy)"/>
  <text x="933" y="528" font-size="10" font-weight="700" fill="#00113A" text-anchor="middle">implements →</text>

  <!-- Critic -> Actor : lower channel (reject/revise), distinct lower port, orthogonal -->
  <path d="M988,584 H 880" stroke="#C0392B" stroke-width="2.2" fill="none" marker-end="url(#aRed)"/>
  <text x="934" y="600" font-size="10" font-weight="700" fill="#C0392B" text-anchor="middle">← reject · revise</text>

  <!-- rubric chips along the bottom of the loop container -->
  <text x="700" y="640" font-size="10" font-weight="700" fill="#64748B">RUBRIC</text>
  <g font-size="9.5" font-weight="600" fill="#334155">
    <rect x="758" y="628" width="76" height="20" rx="10" fill="#EEF1F6"/><text x="796" y="642" text-anchor="middle">security</text>
    <rect x="842" y="628" width="100" height="20" rx="10" fill="#EEF1F6"/><text x="892" y="642" text-anchor="middle">error-handling</text>
    <rect x="950" y="628" width="84" height="20" rx="10" fill="#EEF1F6"/><text x="992" y="642" text-anchor="middle">edge-cases</text>
    <rect x="1042" y="628" width="80" height="20" rx="10" fill="#EEF1F6"/><text x="1082" y="642" text-anchor="middle">dead-code</text>
  </g>

  <!-- ============ DOX callout tied to Actor (left of the loop) ============ -->
  <g filter="url(#softShadow)"><rect x="346" y="512" width="252" height="84" rx="12" fill="#FFFFFF" stroke="#E1E5EE" stroke-width="1.5"/></g>
  <text x="364" y="536" font-size="11" font-weight="700" letter-spacing="0.8" fill="#0046F5">DOX · AGENTS.md HIERARCHY</text>
  <text x="364" y="557" font-size="9.5" fill="#64748B">before: read the nearest AGENTS.md chain</text>
  <text x="364" y="573" font-size="9.5" fill="#64748B">after: update the owning AGENTS.md when a</text>
  <text x="364" y="588" font-size="9.5" fill="#64748B">change alters contracts or structure</text>
  <!-- DOX -> loop : orthogonal horizontal connector anchored on the DOX card's right -->
  <!-- edge (x=598, y=554 = mid band between header and body, no text occluded). Lands -->
  <!-- its arrowhead on the blue container's OUTER left edge (x=674) — does not pierce -->
  <!-- the frame, does not reach the Actor card. -->
  <path d="M598,554 H 672" stroke="#94A3B8" stroke-width="2" fill="none" marker-end="url(#aGrey)"/>

  <!-- ============ DETERMINISTIC GATE CALLOUT (right of the loop) ============ -->
  <!-- shrunk to fit content with comfortable padding (no dead space) -->
  <g filter="url(#softShadow)"><rect x="1244" y="455" width="212" height="92" rx="14" fill="#FFFFFF" stroke="#0F9D58" stroke-width="1.5"/></g>
  <text x="1262" y="478" font-size="10.5" font-weight="700" letter-spacing="0.4" fill="#0F9D58">DETERMINISTIC COMMIT GATE</text>
  <text x="1262" y="500" font-size="12.5" font-weight="700" fill="#00113A">exit code, not opinion</text>
  <text x="1262" y="519" font-size="10" fill="#0F9D58">tests pass → commit</text>
  <text x="1262" y="535" font-size="10" fill="#C0392B">tests fail → blocked, no commit</text>
  <!-- connector from gate diamond straight down onto the callout top edge (clean port) -->
  <path d="M1246,271 V 392 H 1350 V 453" stroke="#0F9D58" stroke-width="1.8" stroke-dasharray="4 4" fill="none" marker-end="url(#aGreen)"/>

  <!-- ===================================================================== -->
  <!-- ============ L3 PROJECT MEMORY LEARNING LOOP (HERO) — orthogonal ============ -->
  <!-- bottom-center memory store, generous gutters above (loop bottom 667) ============ -->
  <!-- ===================================================================== -->
  <!-- Memory store node, bottom-center -->
  <g filter="url(#cardShadow)">
    <rect x="540" y="848" width="460" height="96" rx="16" fill="#0F9D58"/>
  </g>
  <text x="566" y="876" font-size="10" font-weight="700" letter-spacing="0.7" fill="#FFFFFF">L3 · PROJECT MEMORY — COMMITTED TO YOUR REPO</text>
  <text x="566" y="905" font-size="18" font-weight="700" fill="#FFFFFF">Learnings + ADRs</text>
  <text x="566" y="928" font-size="11" fill="#FFFFFF" opacity="0.92">from bug-diagnosis · recurring-review · post-mortem</text>

  <!-- OUT arc: Commit -> Memory store. Drops a short way from Commit, jogs RIGHT into -->
  <!-- the far-right channel (x=1484) ABOVE the gate callout (top y=455), then straight -->
  <!-- down past the callout and left into the memory store — clears the callout box -->
  <!-- entirely (no line crosses the callout text). Single clean right-angle routing. -->
  <path d="M1366,286 V 420 H 1484 V 896 H 1002" stroke="#0F9D58" stroke-width="3" fill="none" marker-end="url(#aGreen)"/>
  <rect x="1086" y="884" width="232" height="24" rx="12" fill="#FFFFFF" stroke="#0F9D58" stroke-width="1.5"/>
  <text x="1202" y="900" font-size="10" font-weight="600" fill="#0F9D58" text-anchor="middle">every run records what it learned</text>

  <!-- BACK arc: Memory store -> Scout/Spec (injected). Orthogonal trunk: -->
  <!-- left along the bottom to x=320 (clear gutter just LEFT of the L2 band and RIGHT -->
  <!-- of the graphify card), up that center-left channel to y=323, then a horizontal -->
  <!-- trunk at y=323 that taps risers up into Scout (x=410) and Spec (x=578). -->
  <!-- y=323 sits in the 13px gap just below the band's dotted bottom (y=316), -->
  <!-- so the green segment is distinct from the band border. -->
  <path d="M540,896 H 320 V 323 H 578" stroke="#0F9D58" stroke-width="3" fill="none"/>
  <!-- riser into Scout bottom (arrowhead lands flush on the card bottom edge y=286) -->
  <path d="M410,323 V 286" stroke="#0F9D58" stroke-width="3" fill="none" marker-end="url(#aGreen)"/>
  <!-- riser into Spec bottom (trunk end turns up here, lands flush on the edge y=286) -->
  <path d="M578,323 V 286" stroke="#0F9D58" stroke-width="3" fill="none" marker-end="url(#aGreen)"/>
  <!-- injected label: green pill sitting ON the center-left injection channel (x=320) -->
  <rect x="214" y="470" width="212" height="24" rx="12" fill="#0F9D58"/>
  <text x="320" y="486" font-size="10" font-weight="700" fill="#FFFFFF" text-anchor="middle">injected into future runs</text>

  <!-- L4 guardrails: injected alongside memory at Scout/Spec. Placed right of the -->
  <!-- green memory channel so the trunk never crosses it; a short grey connector -->
  <!-- merges left into the injection channel (x=320). -->
  <g filter="url(#softShadow)"><rect x="346" y="680" width="252" height="66" rx="12" fill="#FFFFFF" stroke="#CBD5E1" stroke-width="1.5"/></g>
  <text x="364" y="704" font-size="10.5" font-weight="700" letter-spacing="0.6" fill="#64748B">L4 · GUARDRAILS</text>
  <text x="364" y="723" font-size="9.5" fill="#64748B">universal AI-to-AI constraints,</text>
  <text x="364" y="736" font-size="9.5" fill="#64748B">injected with memory at Scout / Spec</text>
  <!-- guardrails -> TEE-joins the green injection channel at x=320 (no arrowhead: a -->
  <!-- deliberate merge junction). A small green junction dot marks the merge point. -->
  <path d="M346,713 H 320" stroke="#0F9D58" stroke-width="2" fill="none"/>
  <circle cx="320" cy="713" r="4" fill="#0F9D58"/>

  <!-- headline tag on the loop -->
  <text x="660" y="772" font-size="15" font-weight="700" fill="#0F9D58">It gets smarter every run.</text>
  <text x="660" y="794" font-size="11" fill="#64748B">the memory loop is what makes it different from a plain AI assistant</text>

  <!-- injection target tags on Scout & Spec headers -->
  <g font-size="8.5" font-weight="700" fill="#0F9D58">
    <rect x="378" y="208" width="64" height="15" rx="7.5" fill="#E4F5E6"/>
    <text x="410" y="219" text-anchor="middle">+ memory · L4</text>
    <rect x="546" y="208" width="64" height="15" rx="7.5" fill="#E4F5E6"/>
    <text x="578" y="219" text-anchor="middle">+ memory · L4</text>
  </g>

  <!-- ===================================================================== -->
  <!-- ============ LAYER LEGEND (bottom, centered & evenly spread) ============ -->
  <!-- ===================================================================== -->
  <g font-size="10">
    <text x="64" y="1002" font-weight="700" fill="#64748B">LAYERS</text>
    <rect x="160" y="992" width="14" height="14" rx="3" fill="#00113A"/><text x="180" y="1003" fill="#334155">L1 Plugin shell</text>
    <rect x="380" y="992" width="14" height="14" rx="3" fill="#0046F5"/><text x="400" y="1003" fill="#334155">L2 Workflow engine</text>
    <rect x="620" y="992" width="14" height="14" rx="3" fill="#0F9D58"/><text x="640" y="1003" fill="#334155">L3 Project memory</text>
    <rect x="860" y="992" width="14" height="14" rx="3" fill="#CBD5E1"/><text x="880" y="1003" fill="#334155">L4 Guardrails</text>
    <rect x="1080" y="992" width="14" height="14" rx="3" fill="#FFFFFF" stroke="#94A3B8" stroke-width="1.3" stroke-dasharray="3 2"/><text x="1100" y="1003" fill="#334155">Optional (graphify)</text>
  </g>

  <!-- ============ FOOTER STAMP ============ -->
  <text x="1456" y="1030" font-size="10" font-style="italic" fill="#94A3B8" text-anchor="end">dev-squad v2.0.35 · adversarial actor⇄critic · deterministic gate · project memory</text>
</svg>
